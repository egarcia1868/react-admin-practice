import * as React from "react";
import { Card, CardContent, CardHeader } from "@material-ui/core";

const Dashboard = () => (
  <Card>
    <CardHeader title="Welcome to Eric's demo" />
    <CardContent>Here's some words to display on the dashboard...</CardContent>
  </Card>
)

export default Dashboard;